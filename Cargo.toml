[package]
name = "hestia"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Nix binary cache backed by the GitHub Actions cache"
repository = "https://github.com/nix-community/hestia"

[dependencies]
clap = { version = "4", features = ["derive"] }
